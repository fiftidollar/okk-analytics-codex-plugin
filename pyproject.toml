[tool.pytest.ini_options]
testpaths = ["server/tests"]
addopts = "-q"

[tool.ruff]
target-version = "py312"
line-length = 110
src = ["server"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "ASYNC"]
ignore = ["E501", "B008"]
